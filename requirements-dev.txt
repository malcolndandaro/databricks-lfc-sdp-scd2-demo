# Dev / test dependencies for the Direct-Sales E2E demo.
# Pinned loosely — patch versions float to pick up security fixes.
#
# These are NOT runtime dependencies of any pipeline (SDP runs everything
# server-side). They are: (1) the synthetic data generator, (2) pytest
# tests run against serverless via databricks-connect, and (3) reset
# / upload helper scripts.

# Synthetic data generator
faker>=25.0,<30
numpy>=1.26,<3
pyarrow>=15,<20

# Tests
pytest>=8,<9
databricks-connect>=15,<17

# SDK (used by the volume uploader + reset scripts)
databricks-sdk>=0.30,<1

# Pure-Python TDS client used by scripts/reset_sql_server.py to TRUNCATE
# crm.consultoras locally before triggering the sqlserver_setup Job.
# Pure-Python so no ODBC drivers or libsybdb need to be installed.
python-tds>=1.13,<2
